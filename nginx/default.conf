upstream django {
    server loansystem:8000;
}

server {
    listen 80;
    server_name loansystem;
    
    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        alias /static/;
    }
}
